# Application database connection (least-privilege role)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=grad_data
DB_USER=grad_app
DB_PASSWORD=change_me

# Optional admin connection for first-time database provisioning
DB_ADMIN_HOST=localhost
DB_ADMIN_PORT=5432
DB_ADMIN_NAME=postgres
DB_ADMIN_USER=postgres
DB_ADMIN_PASSWORD=change_me_admin

# Optional DSN overrides (if set, these take precedence)
# DATABASE_URL=
# DATABASE_ADMIN_URL=
